var Folder = require("../models/folders");
function folders(uid) {
  console.log(global.uid);
}
function folderInfo(folderId) {}
function addFolder(folderInfo) {}
function delFolder(folderId) {}
module.exports = { folders, folderInfo, addFolder };
